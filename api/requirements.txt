# API FastAPI — dependências específicas
# As dependências do core/ estão em ../requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-jose[cryptography]>=3.3.0  # verificação JWT Supabase
httpx>=0.27.0                      # cliente HTTP assíncrono
python-multipart>=0.0.12           # upload de arquivos (extratos PDF)
