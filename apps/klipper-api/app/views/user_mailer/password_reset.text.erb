Olá <%= @user.name %>,

Recebemos uma solicitação para redefinir a senha da sua conta Klipper.

Para criar uma nova senha, acesse o link abaixo (válido por 30 minutos):

<%= @reset_url %>

Se você não solicitou isso, pode ignorar este e-mail com segurança — sua senha continua a mesma.
