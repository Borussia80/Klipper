# Registro de dívida técnica

> Arquivo gerado automaticamente por `.github/scripts/render-report.mjs` a
> partir de `reports/registry/findings.json`, a cada execução de
> `agent-full-report.yml`. **Não editar manualmente** — qualquer edição direta
> é sobrescrita na próxima execução. Para corrigir um item, resolva o finding
> no código (o script marca como resolvido quando ele deixa de aparecer nas
> execuções seguintes) ou ajuste o registro em `reports/registry/findings.json`.

Nenhuma execução do relatório completo ainda. Esta tabela é preenchida após o
primeiro run de `agent-full-report.yml`.

| ID | Categoria | Descrição | Criado em | Última ocorrência | Impacto | Esforço | Prioridade |
|----|-----------|-----------|-----------|--------------------|---------|---------|------------|
